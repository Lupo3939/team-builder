
// TeamCompDesigner.cpp : Defines the entry point for the console application.
//

#include <string>
#include <vector>
#include <unordered_map>
#include <iostream>
#include <fstream>
#include <algorithm>
using namespace std;

vector<string> Champions = {"Aatrox", "Ahri", "Akali", "Alistar", "Amumu", "Anivia", "Annie", "Ashe", "Aurelion Sol", "Azir", "Bard", "Blitzcrank", "Brand", "Braum", "Caitlyn", "Camille", "Cassiopeia", "Cho'Gath", "Corki", "Darius", "Diana", "Dr. Mundo",
		"Draven", "Ekko", "Elise", "Evelynn", "Ezreal", "Fiddlesticks", "Fiora", "Fizz", "Galio", "Gangplank", "Garen", "Gnar", "Gragas", "Graves", "Hecarim", "Heimerdinger", "Illaoi", "Irelia", "Ivern", "Janna", "Jarvan IV", "Jax", "Jayce", 
		"Jhin", "Jinx", "Kalista", "Karma", "Karthus", "Kassadin", "Katarina", "Kayle", "Kayn", "Kennen", "Kha'Zix", "Kindred", "Kled", "Kog'Maw", "Leblanc", "Lee Sin", "Leona", "Lissandra", "Lucian", "Lulu", "Lux", "Malphite", "Malzahar", "Maokai", 
		"Master Yi", "Miss Fortune", "Mordekaiser", "Morgana", "Nami", "Nasus", "Nautilus", "Nidalee", "Nocturne", "Nunu", "Olaf", "Orianna", "Pantheon", "Poppy", "Quinn", "Rakan", "Rammus", "Rek'Sai", "Renekton", "Rengar", "Riven", "Rumble",
		"Ryze", "Sejuani", "Shaco", "Shen", "Shyvana", "Singed", "Sion", "Sivir", "Skarner", "Sona", "Soraka", "Swain", "Syndra", "Tahm Kench", "Taliyah", "Talon", "Taric","Teemo", "Thresh", "Tristana", "Trundle", "Tryndamere", "Twisted Fate", "Twitch",
		"Udyr", "Urgot", "Varus", "Vayne", "Veigar", "Vel'Koz", "Vi", "Viktor", "Vladimir", "Volibear", "Warwick", "Wukong", "Xayah", "Xerath", "Xin Zhao", "Yasuo", "Yorick", "Zac", "Zed", "Ziggs", "Zilean", "Zyra"};

		//^Pool of Current Champions in League of Legends

struct Champion {
	string name;
	int tank;
	int range;
	int cc;
	int mobility;
	int damage;
	int heal;
	int shield;
};


vector<Champion> Champs;

struct Player { //Pool of Players
	string name;
	vector<string> pool;
};

void initializeChamps(){ //Load recent data for each Champ

    string line;
    int tank;
    int range;
    int cc;
    int mobility;
    int damage;
    int heal;
    int shield;
    int x;
    
    ifstream myfile("read-champs");
    int i = 0;
    
	//Aatrox
Champion Aatrox;
    getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    
    Aatrox.name = Champions[i];
    Aatrox.tank = tank;
    Aatrox.range = range;
    Aatrox.cc = cc;
    Aatrox.mobility = mobility;
    Aatrox.damage = damage;
    Aatrox.heal = heal;
    Aatrox.shield = shield;
    
    i++;
    
    Champs.push_back(Aatrox);
    
    //Ahri
    Champion Ahri;
    getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Ahri.name = Champions[i];
    Ahri.tank = tank;
    Ahri.range = range;
    Ahri.cc = cc;
    Ahri.mobility = mobility;
    Ahri.damage = damage;
    Ahri.heal = heal;
    Ahri.shield = shield;
    
    i++;
    Champs.push_back(Ahri);
    
	//Akali
    Champion Akali;
	getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Akali.name = Champions[i];
    Akali.tank = tank;
    Akali.range = range;
    Akali.cc = cc;
    Akali.mobility = mobility;
    Akali.damage = damage;
    Akali.heal = heal;
    Akali.shield = shield;    
    
    i++;
    Champs.push_back(Akali);
    
	//Alistar
    Champion Alistar;
	getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Alistar.name = Champions[i];
    Alistar.tank = tank;
    Alistar.range = range;
    Alistar.cc = cc;
    Alistar.mobility = mobility;
    Alistar.damage = damage;
    Alistar.heal = heal;
    Alistar.shield = shield;
    
    i++;
    Champs.push_back(Alistar);
    
	//Amumu
    Champion Amumu;
	getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Amumu.name = Champions[i];
    Amumu.tank = tank;
    Amumu.range = range;
    Amumu.cc = cc;
    Amumu.mobility = mobility;
    Amumu.damage = damage;
    Amumu.heal = heal;
    Amumu.shield = shield;
     
    i++;
    Champs.push_back(Amumu);
    
	//Anivia
    Champion Anivia;
		getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Anivia.name = Champions[i];
    Anivia.tank = tank;
    Anivia.range = range;
    Anivia.cc = cc;
    Anivia.mobility = mobility;
    Anivia.damage = damage;
    Anivia.heal = heal;
    Anivia.shield = shield;
    
    i++;
    Champs.push_back(Anivia);
    
	//Annie
    Champion Annie;
		getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Annie.name = Champions[i];
    Annie.tank = tank;
    Annie.range = range;
    Annie.cc = cc;
    Annie.mobility = mobility;
    Annie.damage = damage;
    Annie.heal = heal;
    Annie.shield = shield;
    
    i++;
    Champs.push_back(Annie);
    
	//Ashe
    Champion Ashe;
    Ashe.name = Champions[i];
    Ashe.tank = tank;
    Ashe.range = range;
    Ashe.cc = cc;
    Ashe.mobility = mobility;
    Ashe.damage = damage;
    Ashe.heal = heal;
    Ashe.shield = shield;
    
    i++;
    Champs.push_back(Ashe);
    
	//Aurelion Sol
    Champion Aurelion;
    Aurelion.name = Champions[i];
    Aurelion.tank = tank;
    Aurelion.range = range;
    Aurelion.cc = cc;
    Aurelion.mobility = mobility;
    Aurelion.damage = damage;
    Aurelion.heal = heal;
    Aurelion.shield = shield;
    
    i++;
    Champs.push_back(Aurelion);
    
	//Azir
    Champion Azir;
	getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Azir.name = Champions[i];
    Azir.tank = tank;
    Azir.range = range;
    Azir.cc = cc;
    Azir.mobility = mobility;
    Azir.damage = damage;
    Azir.heal = heal;
    Azir.shield = shield;
    
    i++;
    Champs.push_back(Azir);
    
	//Bard
    Champion Bard;
	getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Bard.name = Champions[i];
    Bard.tank = tank;
    Bard.range = range;
    Bard.cc = cc;
    Bard.mobility = mobility;
    Bard.damage = damage;
    Bard.heal = heal;
    Bard.shield = shield;
    
    i++;
    Champs.push_back(Bard);
    
    Champion Blitzcrank;
	getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Blitzcrank.name = Champions[i];
    Blitzcrank.tank = tank;
    Blitzcrank.range = range;
    Blitzcrank.cc = cc;
    Blitzcrank.mobility = mobility;
    Blitzcrank.damage = damage;
    Blitzcrank.heal = heal;
    Blitzcrank.shield = shield;
    
    i++;
    Champs.push_back(Blitzcrank);
    
    Champion Brand;
	getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Brand.name = Champions[i];
    Brand.tank = tank;
    Brand.range = range;
    Brand.cc = cc;
    Brand.mobility = mobility;
    Brand.damage = damage;
    Brand.heal = heal;
    Brand.shield = shield;
    
    i++;
    Champs.push_back(Brand);
    
    Champion Braum;	
	getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Braum.name = Champions[i];
    Braum.tank = tank;
    Braum.range = range;
    Braum.cc = cc;
    Braum.mobility = mobility;
    Braum.damage = damage;
    Braum.heal = heal;
    Braum.shield = shield;
    
    i++;
    Champs.push_back(Braum);
    
	//Caitlyn
	Champion Caitlyn;
	getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Caitlyn.name = Champions[i];
    Caitlyn.tank = tank;
    Caitlyn.range = range;
    Caitlyn.cc = cc;
    Caitlyn.mobility = mobility;
    Caitlyn.damage = damage;
    Caitlyn.heal = heal;
    Caitlyn.shield = shield;
    
    i++;
    Champs.push_back(Caitlyn);
    
	//Camille
    Champion Camille;
	getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Camille.name = Champions[i];
    Camille.tank = tank;
    Camille.range = range;
    Camille.cc = cc;
    Camille.mobility = mobility;
    Camille.damage = damage;
    Camille.heal = heal;
    Camille.shield = shield;
    
    i++;
    Champs.push_back(Camille);
    
	//Cassiopeia
    Champion Cass;
	getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Cass.name = Champions[i];
    Cass.tank = tank;
    Cass.range = range;
    Cass.cc = cc;
    Cass.mobility = mobility;
    Cass.damage = damage;
    Cass.heal = heal;
    Cass.shield = shield;
    
    i++;
    Champs.push_back(Cass);
        
	//Cho'Gath
    Champion Cho;
	getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Cho.name = Champions[i];
    Cho.tank = tank;
    Cho.range = range;
    Cho.cc = cc;
    Cho.mobility = mobility;
    Cho.damage = damage;
    Cho.heal = heal;
    Cho.shield = shield;
    
    i++;
    Champs.push_back(Cho);
    
	//Corki
    Champion Corki;
	getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Corki.name = Champions[i];
    Corki.tank = tank;
    Corki.range = range;
    Corki.cc = cc;
    Corki.mobility = mobility;
    Corki.damage = damage;
    Corki.heal = heal;
    Corki.shield = shield;
    
    i++;
    Champs.push_back(Corki);
	
	Champion Darius;
    Darius.name = Champions[i];
    Darius.name = ;
    Darius.tank = tank;
    Darius.range = range;
    Darius.cc = cc;
    Darius.mobility = mobility;
    Darius.damage = damage;
    Darius.heal = heal;
    Darius.shield = shield;
    
    i++;
    Champion Darius;
    Darius.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Darius.name = ;
    Darius.tank = tank;
    Darius.range = range;
    Darius.cc = cc;
    Darius.mobility = mobility;
    Darius.damage = damage;
    Darius.heal = heal;
    Darius.shield = shield;
    
    i++;
    Champs.push_back(Darius);
    
    Champion Diana;
    Diana.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Diana.name = ;
    Diana.tank = tank;
    Diana.range = range;
    Diana.cc = cc;
    Diana.mobility = mobility;
    Diana.damage = damage;
    Diana.heal = heal;
    Diana.shield = shield;
    
    i++;
    Champs.push_back(Diana);
	
	Champion Mundo;
    Mundo.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Mundo.name = ;
    Mundo.tank = tank;
    Mundo.range = range;
    Mundo.cc = cc;
    Mundo.mobility = mobility;
    Mundo.damage = damage;
    Mundo.heal = heal;
    Mundo.shield = shield;
    
    i++;
    Champs.push_back(Mundo);
    
    Champion Draven;
    Draven.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Draven.name = ;
    Draven.tank = tank;
    Draven.range = range;
    Draven.cc = cc;
    Draven.mobility = mobility;
    Draven.damage = damage;
    Draven.heal = heal;
    Draven.shield = shield;
    
    i++;
    Champs.push_back(Draven);
        
    Champion Ekko;
    Ekko.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Ekko.name = ;
    Ekko.tank = tank;
    Ekko.range = range;
    Ekko.cc = cc;
    Ekko.mobility = mobility;
    Ekko.damage = damage;
    Ekko.heal = heal;
    Ekko.shield = shield;
    
    i++;
    Champs.push_back(Ekko);
    
    Champion Elise;
    Elise.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Elise.name = ;
    Elise.tank = tank;
    Elise.range = range;
    Elise.cc = cc;
    Elise.mobility = mobility;
    Elise.damage = damage;
    Elise.heal = heal;
    Elise.shield = shield;
    
    i++;
    Champs.push_back(Elise);
	
	Champion Eve;
    Eve.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Eve.name = ;
    Eve.tank = tank;
    Eve.range = range;
    Eve.cc = cc;
    Eve.mobility = mobility;
    Eve.damage = damage;
    Eve.heal = heal;
    Eve.shield = shield;
    
    i++;
    Champs.push_back(Eve);
    
    Champion Ez;
    Ez.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Ez.name = ;
    Ez.tank = tank;
    Ez.range = range;
    Ez.cc = cc;
    Ez.mobility = mobility;
    Ez.damage = damage;
    Ez.heal = heal;
    Ez.shield = shield;
    
    i++;
    Champs.push_back(Ez);
	
	Champion Fiddle;
    Fiddle.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Fiddle.name = ;
    Fiddle.tank = tank;
    Fiddle.range = range;
    Fiddle.cc = cc;
    Fiddle.mobility = mobility;
    Fiddle.damage = damage;
    Fiddle.heal = heal;
    Fiddle.shield = shield;
    
    i++;
    Champs.push_back(Fiddle);
    
    Champion Fiora;
    Fiora.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Fiora.name = ;
    Fiora.tank = tank;
    Fiora.range = range;
    Fiora.cc = cc;
    Fiora.mobility = mobility;
    Fiora.damage = damage;
    Fiora.heal = heal;
    Fiora.shield = shield;
    
    i++;
    Champs.push_back(Fiora);
	
	Champion Fizz;
    Fizz.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Fizz.name = ;
    Fizz.tank = tank;
    Fizz.range = range;
    Fizz.cc = cc;
    Fizz.mobility = mobility;
    Fizz.damage = damage;
    Fizz.heal = heal;
    Fizz.shield = shield;
    
    i++;
    Champs.push_back(Fizz);
    
    Champion Galio;
    Galio.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Galio.name = ;
    Galio.tank = tank;
    Galio.range = range;
    Galio.cc = cc;
    Galio.mobility = mobility;
    Galio.damage = damage;
    Galio.heal = heal;
    Galio.shield = shield;
    
    i++;
    Champs.push_back(Galio);
        
    Champion GP;
    GP.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Gp.name = ;
    GP.tank = tank;
    GP.range = range;
    GP.cc = cc;
    GP.mobility = mobility;
    GP.damage = damage;
    GP.heal = heal;
    GP.shield = shield;
    
    i++;
    Champs.push_back(GP);
    
    Champion Garen;
    Garen.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Garen.name = ;
    Garen.tank = tank;
    Garen.range = range;
    Garen.cc = cc;
    Garen.mobility = mobility;
    Garen.damage = damage;
    Garen.heal = heal;
    Garen.shield = shield;
    
    i++;
    Champs.push_back(Garen);
	
	Champion Gnar;
    Gnar.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Gnar.name = ;
    Gnar.tank = tank;
    Gnar.range = range;
    Gnar.cc = cc;
    Gnar.mobility = mobility;
    Gnar.damage = damage;
    Gnar.heal = heal;
    Gnar.shield = shield;
    
    i++;
    Champs.push_back(Gnar);
    
    Champion Gragas;
    Gragas.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Gragas.name = ;
    Gragas.tank = tank;
    Gragas.range = range;
    Gragas.cc = cc;
    Gragas.mobility = mobility;
    Gragas.damage = damage;
    Gragas.heal = heal;
    Gragas.shield = shield;
    
    i++;
    Champs.push_back(Gragas);

    Champion Graves;
    Graves.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Graves.name = ;
    Graves.tank = tank;
    Graves.range = range;
    Graves.cc = cc;
    Graves.mobility = mobility;
    Graves.damage = damage;
    Graves.heal = heal;
    Graves.shield = shield;
    
    i++;
    Champs.push_back(Graves);
    
    Champion Hecarim;
    Hecarim.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Hecarim.name = ;
    Hecarim.tank = tank;
    Hecarim.range = range;
    Hecarim.cc = cc;
    Hecarim.mobility = mobility;
    Hecarim.damage = damage;
    Hecarim.heal = heal;
    Hecarim.shield = shield;
    
    i++;
    Champs.push_back(Hecarim);
    
    Champion Donger;
    Donger.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Donger.name = ;
    Donger.tank = tank;
    Donger.range = range;
    Donger.cc = cc;
    Donger.mobility = mobility;
    Donger.damage = damage;
    Donger.heal = heal;
    Donger.shield = shield;
    
    i++;
    Champs.push_back(Donger);
    
    Champion Illaoi;
    Illaoi.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Illaoi.name = ;
    Illaoi.tank = tank;
    Illaoi.range = range;
    Illaoi.cc = cc;
    Illaoi.mobility = mobility;
    Illaoi.damage = damage;
    Illaoi.heal = heal;
    Illaoi.shield = shield;
    
    i++;
    Champs.push_back(Illaoi);
    
    Champion Irelia;
    Irelia.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Irelia.name = ;
    Irelia.tank = tank;
    Irelia.range = range;
    Irelia.cc = cc;
    Irelia.mobility = mobility;
    Irelia.damage = damage;
    Irelia.heal = heal;
    Irelia.shield = shield;
    
    i++;
    Champs.push_back(Irelia);
    
    Champion Ivern;
    Ivern.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Ivern.name = ;
    Ivern.tank = tank;
    Ivern.range = range;
    Ivern.cc = cc;
    Ivern.mobility = mobility;
    Ivern.damage = damage;
    Ivern.heal = heal;
    Ivern.shield = shield;
    
    i++;
    Champs.push_back(Ivern);
    
    Champion Janna;
    Janna.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Janna.name = ;
    Janna.tank = tank;
    Janna.range = range;
    Janna.cc = cc;
    Janna.mobility = mobility;
    Janna.damage = damage;
    Janna.heal = heal;
    Janna.shield = shield;
    
    i++;
    Champs.push_back(Janna);
    
    Champion J4;
    J4.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    J4.name = ;
    J4.tank = tank;
    J4.range = range;
    J4.cc = cc;
    J4.mobility = mobility;
    J4.damage = damage;
    J4.heal = heal;
    J4.shield = shield;
    
    i++;
    Champs.push_back(J4);
    
    Champion Jax;
    Jax.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Jax.name = ;
    Jax.tank = tank;
    Jax.range = range;
    Jax.cc = cc;
    Jax.mobility = mobility;
    Jax.damage = damage;
    Jax.heal = heal;
    Jax.shield = shield;
    
    i++;
    Champs.push_back(Jax);
    
    Champion Jayce;
    Jayce.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Jayce.name = ;
    Jayce.tank = tank;
    Jayce.range = range;
    Jayce.cc = cc;
    Jayce.mobility = mobility;
    Jayce.damage = damage;
    Jayce.heal = heal;
    Jayce.shield = shield;
    
    i++;
    Champs.push_back(Jayce);
    
    Champion Jhin;
    Jhin.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Jhin.name = ;
    Jhin.tank = tank;
    Jhin.range = range;
    Jhin.cc = cc;
    Jhin.mobility = mobility;
    Jhin.damage = damage;
    Jhin.heal = heal;
    Jhin.shield = shield;
    
    i++;
    Champs.push_back(Jhin);
    
    Champion Jinx;
    Jinx.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Jinx.name = ;
    Jinx.tank = tank;
    Jinx.range = range;
    Jinx.cc = cc;
    Jinx.mobility = mobility;
    Jinx.damage = damage;
    Jinx.heal = heal;
    Jinx.shield = shield;
    
    i++;
    Champs.push_back(Jinx);
    
    Champion Kalista;
    Kalista.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Kalista.name = ;
    Kalista.tank = tank;
    Kalista.range = range;
    Kalista.cc = cc;
    Kalista.mobility = mobility;
    Kalista.damage = damage;
    Kalista.heal = heal;
    Kalista.shield = shield;
    
    i++;
    Champs.push_back(Kalista);
    
    Champion Karma;
    Karma.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Karma.name = ;
    Karma.tank = tank;
    Karma.range = range;
    Karma.cc = cc;
    Karma.mobility = mobility;
    Karma.damage = damage;
    Karma.heal = heal;
    Karma.shield = shield;
    
    i++;
    Champs.push_back(Karma);
    
    Champion Karthus;
    Karthus.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Karthus.name = ;
    Karthus.tank = tank;
    Karthus.range = range;
    Karthus.cc = cc;
    Karthus.mobility = mobility;
    Karthus.damage = damage;
    Karthus.heal = heal;
    Karthus.shield = shield;
    
    i++;
    Champs.push_back(Karthus);
    
    Champion Kassadin;
    Kassadin.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Kassadin.name = ;
    Kassadin.tank = tank;
    Kassadin.range = range;
    Kassadin.cc = cc;
    Kassadin.mobility = mobility;
    Kassadin.damage = damage;
    Kassadin.heal = heal;
    Kassadin.shield = shield;
    
    i++;
    Champs.push_back(Kassadin);
    
    Champion Kat;
    Kat.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Kat.name = ;
    Kat.tank = tank;
    Kat.range = range;
    Kat.cc = cc;
    Kat.mobility = mobility;
    Kat.damage = damage;
    Kat.heal = heal;
    Kat.shield = shield;
    
    i++;
    Champs.push_back(Kat);
    
    Champion Kayle;
    Kayle.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Kayle.name = ;
    Kayle.tank = tank;
    Kayle.range = range;
    Kayle.cc = cc;
    Kayle.mobility = mobility;
    Kayle.damage = damage;
    Kayle.heal = heal;
    Kayle.shield = shield;
    
    i++;
    Champs.push_back(Kayle);
    
    Champion Kayn;
    Kayn.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Kayn.name = ;
    Kayn.tank = tank;
    Kayn.range = range;
    Kayn.cc = cc;
    Kayn.mobility = mobility;
    Kayn.damage = damage;
    Kayn.heal = heal;
    Kayn.shield = shield;
    
    i++;
    Champs.push_back(Kayn);
    
    Champion Kennen;
    Kennen.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Kennen.name = ;
    Kennen.tank = tank;
    Kennen.range = range;
    Kennen.cc = cc;
    Kennen.mobility = mobility;
    Kennen.damage = damage;
    Kennen.heal = heal;
    Kennen.shield = shield;
    
    i++;
    Champs.push_back(Kennen);
    
    Champion Kha;
    Kha.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Kha.name = ;
    Kha.tank = tank;
    Kha.range = range;
    Kha.cc = cc;
    Kha.mobility = mobility;
    Kha.damage = damage;
    Kha.heal = heal;
    Kha.shield = shield;
    
    i++;
    Champs.push_back(Kha);
    
    Champion Kindred;
    Kindred.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Kindred.name = ;
    Kindred.tank = tank;
    Kindred.range = range;
    Kindred.cc = cc;
    Kindred.mobility = mobility;
    Kindred.damage = damage;
    Kindred.heal = heal;
    Kindred.shield = shield;
    
    i++;
    Champs.push_back(Kindred);
    
    Champion Kled;
    Kled.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Kled.name = ;
    Kled.tank = tank;
    Kled.range = range;
    Kled.cc = cc;
    Kled.mobility = mobility;
    Kled.damage = damage;
    Kled.heal = heal;
    Kled.shield = shield;
    
    i++;
    Champs.push_back(Kled);
    
    Champion Kog;
    Kog.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Kog.name = ;
    Kog.tank = tank;
    Kog.range = range;
    Kog.cc = cc;
    Kog.mobility = mobility;
    Kog.damage = damage;
    Kog.heal = heal;
    Kog.shield = shield;
    
    i++;
    Champs.push_back(Kog);
    
    Champion Leblanc;
    Leblanc.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Leblanc.name = ;
    Leblanc.tank = tank;
    Leblanc.range = range;
    Leblanc.cc = cc;
    Leblanc.mobility = mobility;
    Leblanc.damage = damage;
    Leblanc.heal = heal;
    Leblanc.shield = shield;
    
    i++;
    Champs.push_back(Leblanc);
    
    Champion Lee;
    Lee.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Lee.name = ;
    Lee.tank = tank;
    Lee.range = range;
    Lee.cc = cc;
    Lee.mobility = mobility;
    Lee.damage = damage;
    Lee.heal = heal;
    Lee.shield = shield;
    
    i++;
    Champs.push_back(Lee);
    
    Champion Leo;
    Leo.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Leo.name = ;
    Leo.tank = tank;
    Leo.range = range;
    Leo.cc = cc;
    Leo.mobility = mobility;
    Leo.damage = damage;
    Leo.heal = heal;
    Leo.shield = shield;
    
    i++;
    Champs.push_back(Leo);
    
    Champion Liss;
    Liss.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Liss.name = ;
    Liss.tank = tank;
    Liss.range = range;
    LissLiss.cc = cc;
    Liss.mobility = mobility;
    Liss.damage = damage;
    Liss.heal = heal;
    Liss.shield = shield;
    
    i++;
    Champs.push_back(Liss);
    
    Champion Lucian;
    Lucian.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Lucian.name = ;
    Lucian.tank = tank;
    Lucian.range = range;
    LucianLucian.cc = cc;
    Lucian.mobility = mobility;
    Lucian.damage = damage;
    Lucian.heal = heal;
    Lucian.shield = shield;
    
    i++;
    Champs.push_back(Lucian);
    
    Champion Lulu;
    Lulu.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Lulu.name = ;
    Lulu.tank = tank;
    Lulu.range = range;
    Lulu.cc = cc;
    Lulu.mobility = mobility;
    Lulu.damage = damage;
    Lulu.heal = heal;
    Lulu.shield = shield;
    
    i++;
    Champs.push_back(Lulu);
    
    Champion Lux;
    Lux.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Lux.name = ;
    Lux.tank = tank;
    Lux.range = range;
    Lux.cc = cc;
    Lux.mobility = mobility;
    Lux.damage = damage;
    Lux.heal = heal;
    Lux.shield = shield;
    
    i++;
    Champs.push_back(Lux);
    
    Champion Malphite;
    Malphite.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Malphite.name = ;
    Malphite.tank = tank;
    Malphite.range = range;
    Malphite.cc = cc;
    Malphite.mobility = mobility;
    Malphite.damage = damage;
    Malphite.heal = heal;
    Malphite.shield = shield;
    
    i++;
    Champs.push_back(Malphite);
    
    Champion Malzahar;
    Malzahar.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Malzahar.name = ;
    Malzahar.tank = tank;
    Malzahar.range = range;
    Malzahar.cc = cc;
    Malzahar.mobility = mobility;
    Malzahar.damage = damage;
    Malzahar.heal = heal;
    Malzahar.shield = shield;
    
    i++;
    Champs.push_back(Malzahar);
    
    Champion Tree;
    Tree.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Tree.name = ;
    Tree.tank = tank;
    Tree.range = range;
    Tree.cc = cc;
    Tree.mobility = mobility;
    Tree.damage = damage;
    Tree.heal = heal;
    Tree.shield = shield;
    
    i++;
    Champs.push_back(Tree);
    
    Champion Yi;
    Yi.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Yi.name = ;
    Yi.tank = tank;
    Yi.range = range;
    Yi.cc = cc;
    Yi.mobility = mobility;
    Yi.damage = damage;
    Yi.heal = heal;
    Yi.shield = shield;
    
    i++;
    Champs.push_back(Yi);
    
    Champion MF;
    MF.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    MF.name = ;
    MF.tank = tank;
    MF.range = range;
    MF.cc = cc;
    MF.mobility = mobility;
    MF.damage = damage;
    MF.heal = heal;
    MF.shield = shield;
    
    i++;
    Champs.push_back(MF);
    
    Champion Mord;
    Mord.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Mord.name = ;
    Mord.tank = tank;
    Mord.range = range;
    Mord.cc = cc;
    Mord.mobility = mobility;
    Mord.damage = damage;
    Mord.heal = heal;
    Mord.shield = shield;
    
    i++;
    Champs.push_back(Mord);
    
    Champion Morg;
    Morg.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Morg.name = ;
    Morg.tank = tank;
    Morg.range = range;
    Morg.cc = cc;
    Morg.mobility = mobility;
    Morg.damage = damage;
    Morg.heal = heal;
    Morg.shield = shield;
    
    i++;
    Champs.push_back(Morg);
    
    Champion Nami;
    Nami.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Nami.name = ;
    Nami.tank = tank;
    Nami.range = range;
    Nami.cc = cc;
    Nami.mobility = mobility;
    Nami.damage = damage;
    Nami.heal = heal;
    Nami.shield = shield;
    
    i++;
    Champs.push_back(Nami);
    
    Champion Susan;
    Susan.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Susan.name = ;
    Susan.tank = tank;
    Susan.range = range;
    Susan.cc = cc;
    Susan.mobility = mobility;
    Susan.damage = damage;
    Susan.heal = heal;
    Susan.shield = shield;
    
    i++;
    Champs.push_back(Susan);
    
    Champion Naut;
    Naut.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Naut.name = ;
    Naut.tank = tank;
    Naut.range = range;
    Naut.cc = cc;
    Naut.mobility = mobility;
    Naut.damage = damage;
    Naut.heal = heal;
    Naut.shield = shield;
    
    i++;
    Champs.push_back(Naut);
    
    Champion Nid;
    Nid.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Nid.name = ;
    Nid.tank = tank;
    Nid.range = range;
    Nid.cc = cc;
    Nid.mobility = mobility;
    Nid.damage = damage;
    Nid.heal = heal;
    Nid.shield = shield;
    
    i++;
    Champs.push_back(Nid);
    
    Champion Noct;
    Noct.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Noct.name = ;
    Noct.tank = tank;
    Noct.range = range;
    Noct.cc = cc;
    Noct.mobility = mobility;
    Noct.damage = damage;
    Noct.heal = heal;
    Noct.shield = shield;
    
    i++;
    Champs.push_back(Noct);
    
    Champion Nunu;
    Nunu.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Nunu.name = ;
    Nunu.tank = tank;
    Nunu.range = range;
    Nunu.cc = cc;
    Nunu.mobility = mobility;
    Nunu.damage = damage;
    Nunu.heal = heal;
    Nunu.shield = shield;
    
    i++;
    Champs.push_back(Nunu);
    
    Champion Olaf;
    Olaf.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Olaf.name = ;
    Olaf.tank = tank;
    Olaf.range = range;
    Olaf.cc = cc;
    Olaf.mobility = mobility;
    Olaf.damage = damage;
    Olaf.heal = heal;
    Olaf.shield = shield;
    
    i++;
    Champs.push_back(Olaf);
    
	Champion Ori;
    Ori.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Ori.name = ;
    Ori.tank = tank;
    Ori.range = range;
    Ori.cc = cc;
    Ori.mobility = mobility;
    Ori.damage = damage;
    Ori.heal = heal;
    Ori.shield = shield;
    
    i++;
    Champs.push_back(Ori);
    
    Champion Panth;
    Panth.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Panth.name = ;
    Panth.tank = tank;
    Panth.range = range;
    Panth.cc = cc;
    Panth.mobility = mobility;
    Panth.damage = damage;
    Panth.heal = heal;
    Panth.shield = shield;
    
    i++;
    Champs.push_back(Panth);
    
    Champion Poppy;
    Poppy.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Poppy.name = ;
    Poppy.tank = tank;
    Poppy.range = range;
    Poppy.cc = cc;
    Poppy.mobility = mobility;
    Poppy.damage = damage;
    Poppy.heal = heal;
    Poppy.shield = shield;
    
    i++;
    Champs.push_back(Poppy);
        
    Champion Quinn;
    Quinn.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Quinn.name = ;
    Quinn.tank = tank;
    Quinn.range = range;
    Quinn.cc = cc;
    Quinn.mobility = mobility;
    Quinn.damage = damage;
    Quinn.heal = heal;
    Quinn.shield = shield;
    
    i++;
    Champs.push_back(Quinn);
    
    Champion Rakan;
    Rakan.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Rakan.name = ;
    Rakan.tank = tank;
    Rakan.range = range;
    Rakan.cc = cc;
    Rakan.mobility = mobility;
    Rakan.damage = damage;
    Rakan.heal = heal;
    Rakan.shield = shield;
    
    i++;
    Champs.push_back(Rakan);
	
	Champion Rammus;
    Rammus.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Rammus.name = ;
    Rammus.tank = tank;
    Rammus.range = range;
    Rammus.cc = cc;
    Rammus.mobility = mobility;
    Rammus.damage = damage;
    Rammus.heal = heal;
    Rammus.shield = shield;
    
    i++;
    Champs.push_back(Rammus);
    
    Champion Rek;
    Rek.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Rek.name = ;
    Rek.tank = tank;
    Rek.range = range;
    Rek.cc = cc;
    Rek.mobility = mobility;
    Rek.damage = damage;
    Rek.heal = heal;
    Rek.shield = shield;
    
    i++;
    Champs.push_back(Rek);
	
	Champion Renekton;
    Renekton.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Renekton.name = ;
    Renekton.tank = tank;
    Renekton.range = range;
    Renekton.cc = cc;
    Renekton.mobility = mobility;
    Renekton.damage = damage;
    Renekton.heal = heal;
    Renekton.shield = shield;
    
    i++;
    Champs.push_back(Renekton);
    
    Champion Rengar;
    Rengar.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Rengar.name = ;
    Rengar.tank = tank;
    Rengar.range = range;
    Rengar.cc = cc;
    Rengar.mobility = mobility;
    Rengar.damage = damage;
    Rengar.heal = heal;
    Rengar.shield = shield;
    
    i++;
    Champs.push_back(Rengar);
        
    Champion Riven;
    Riven.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Riven.name = ;
    Riven.tank = tank;
    Riven.range = range;
    Riven.cc = cc;
    Riven.mobility = mobility;
    Riven.damage = damage;
    Riven.heal = heal;
    Riven.shield = shield;
    
    i++;
    Champs.push_back(Riven);
    
    Champion Rumble;
    Rumble.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Rumble.name = ;
    Rumble.tank = tank;
    Rumble.range = range;
    Rumble.cc = cc;
    Rumble.mobility = mobility;
    Rumble.damage = damage;
    Rumble.heal = heal;
    Rumble.shield = shield;
    
    i++;
    Champs.push_back(Rumble);
	
	Champion Ryze;
    Ryze.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Ryze.name = ;
    Ryze.tank = tank;
    Ryze.range = range;
    Ryze.cc = cc;
    Ryze.mobility = mobility;
    Ryze.damage = damage;
    Ryze.heal = heal;
    Ryze.shield = shield;
    
    i++;
    Champs.push_back(Ryze);
    
    Champion Sej;
    Sej.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Sej.name = ;
    Sej.tank = tank;
    Sej.range = range;
    Sej.cc = cc;
    Sej.mobility = mobility;
    Sej.damage = damage;
    Sej.heal = heal;
    Sej.shield = shield;
    
    i++;
    Champs.push_back(Sej);
	
	Champion Shaco;
    Shaco.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Shaco.name = ;
    Shaco.tank = tank;
    Shaco.range = range;
    Shaco.cc = cc;
    Shaco.mobility = mobility;
    Shaco.damage = damage;
    Shaco.heal = heal;
    Shaco.shield = shield;
    
    i++;
    Champs.push_back(Shaco);
    
    Champion Shen;
    Shen.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Shen.name = ;
    Shen.tank = tank;
    Shen.range = range;
    Shen.cc = cc;
    Shen.mobility = mobility;
    Shen.damage = damage;
    Shen.heal = heal;
    Shen.shield = shield;
    
    i++;
    Champs.push_back(Shen);
	
	Champion Shyv;
    Shyv.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Shyv.name = ;
    Shyv.tank = tank;
    Shyv.range = range;
    Shyv.cc = cc;
    Shyv.mobility = mobility;
    Shyv.damage = damage;
    Shyv.heal = heal;
    Shyv.shield = shield;
    
    i++;
    Champs.push_back(Shyv);
    
    Champion Singed;
    Singed.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Singed.name = ;
    Singed.tank = tank;
    Singed.range = range;
    Singed.cc = cc;
    Singed.mobility = mobility;
    Singed.damage = damage;
    Singed.heal = heal;
    Singed.shield = shield;
    
    i++;
    Champs.push_back(Singed);
        
    Champion Sion;
    Sion.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Sion.name = ;
    Sion.tank = tank;
    Sion.range = range;
    Sion.cc = cc;
    Sion.mobility = mobility;
    Sion.damage = damage;
    Sion.heal = heal;
    Sion.shield = shield;
    
    i++;
    Champs.push_back(Sion);
    
    Champion Sivir;
    Sivir.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Sivir.name = ;
    Sivir.tank = tank;
    Sivir.range = range;
    Sivir.cc = cc;
    Sivir.mobility = mobility;
    Sivir.damage = damage;
    Sivir.heal = heal;
    Sivir.shield = shield;
    
    i++;
    Champs.push_back(Sivir);
	
	Champion Skarner;
    Skarner.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Skarner.name = ;
    Skarner.tank = tank;
    Skarner.range = range;
    Skarner.cc = cc;
    Skarner.mobility = mobility;
    Skarner.damage = damage;
    Skarner.heal = heal;
    Skarner.shield = shield;
    
    i++;
    Champs.push_back(Skarner);
    
    Champion Sona;
    Sona.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Sona.name = ;
    Sona.tank = tank;
    Sona.range = range;
    Sona.cc = cc;
    Sona.mobility = mobility;
    Sona.damage = damage;
    Sona.heal = heal;
    Sona.shield = shield;
    
    i++;
    Champs.push_back(Sona);

    Champion Soraka;
    Soraka.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Soraka.name = ;
    Soraka.tank = tank;
    Soraka.range = range;
    Soraka.cc = cc;
    Soraka.mobility = mobility;
    Soraka.damage = damage;
    Soraka.heal = heal;
    Soraka.shield = shield;
    
    i++;
    Champs.push_back(Soraka);
    
    Champion Swain;
    Swain.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Swain.name = ;
    Swain.tank = tank;
    Swain.range = range;
    Swain.cc = cc;
    Swain.mobility = mobility;
    Swain.damage = damage;
    Swain.heal = heal;
    Swain.shield = shield;
    
    i++;
    Champs.push_back(Swain);
    
    Champion Syndra;
    Syndra.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Syndra.tank = tank;
    Syndra.range = range;
    Syndra.cc = cc;
    Syndra.mobility = mobility;
    Syndra.damage = damage;
    Syndra.heal = heal;
    Syndra.shield = shield;
    
    i++;
    Champs.push_back(Syndra);
    
	//Tahm Kench
    Champion Toad;
    Toad.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Toad.tank = tank;
    Toad.range = range;
    Toad.cc = cc;
    Toad.mobility = mobility;
    Toad.damage = damage;
    Toad.heal = heal;
    Toad.shield = shield;
    
    i++;
    Champs.push_back(Toad);
    
	//Taliyah
    Champion Taliyah;
    Taliyah.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Taliyah.tank = tank;
    Taliyah.range = range;
    Taliyah.cc = cc;
    Taliyah.mobility = mobility;
    Taliyah.damage = damage;
    Taliyah.heal = heal;
    Taliyah.shield = shield;
    
    i++;
    Champs.push_back(Taliyah);
    
	//Talon
    Champion Talon;
    Talon.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Talon.tank = tank;
    Talon.range = range;
    Talon.cc = cc;
    Talon.mobility = mobility;
    Talon.damage = damage;
    Talon.heal = heal;
    Talon.shield = shield;
    
    i++;
    Champs.push_back(Talon);
    
	//Taric
    Champion Fabio;
    Fabio.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Fabio.tank = tank;
    Fabio.range = range;
    Fabio.cc = cc;
    Fabio.mobility = mobility;
    Fabio.damage = damage;
    Fabio.heal = heal;
    Fabio.shield = shield;
    
    i++;
    Champs.push_back(Fabio);
    
	//Teemo
    Champion Satan;
    Satan.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Satan.tank = tank;
    Satan.range = range;
    Satan.cc = cc;
    Satan.mobility = mobility;
    Satan.damage = damage;
    Satan.heal = heal;
    Satan.shield = shield;
    
    i++;
    Champs.push_back(Satan);
    
	//Thresh
    Champion Thresh;
    Thresh.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Thresh.tank = tank;
    Thresh.range = range;
    Thresh.cc = cc;
    Thresh.mobility = mobility;
    Thresh.damage = damage;
    Thresh.heal = heal;
    Thresh.shield = shield;
    
    i++;
    Champs.push_back(Thresh);
    
	//Tristana
    Champion Trist;
    Trist.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Trist.tank = tank;
    Trist.range = range;
    Trist.cc = cc;
    Trist.mobility = mobility;
    Trist.damage = damage;
    Trist.heal = heal;
    Trist.shield = shield;
    
    i++;
    Champs.push_back(Trist);
    
	//Trundle
    Champion Trundle;
    Trundle.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Trundle.tank = tank;
    Trundle.range = range;
    Trundle.cc = cc;
    Trundle.mobility = mobility;
    Trundle.damage = damage;
    Trundle.heal = heal;
    Trundle.shield = shield;
    
    i++;
    Champs.push_back(Trundle);
    
	//Tryndamere
    Champion Trynd;
    Trynd.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Trynd.tank = tank;
    Trynd.range = range;
    Trynd.cc = cc;
    Trynd.mobility = mobility;
    Trynd.damage = damage;
    Trynd.heal = heal;
    Trynd.shield = shield;
    
    i++;
    Champs.push_back(Trynd);
    
	//Twisted Fate
    Champion TF;
    TF.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    TF.tank = tank;
    TF.range = range;
    TF.cc = cc;
    TF.mobility = mobility;
    TF.damage = damage;
    TF.heal = heal;
    TF.shield = shield;
    
    i++;
    Champs.push_back(TF);
    
	//Twitch
    Champion Twitch;
    Twitch.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Twitch.tank = tank;
    Twitch.range = range;
    Twitch.cc = cc;
    Twitch.mobility = mobility;
    Twitch.damage = damage;
    Twitch.heal = heal;
    Twitch.shield = shield;
    
    i++;
    Champs.push_back(Twitch);
    
	//Udyr
    Champion Udyr;
    Udyr.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Udyr.tank = tank;
    Udyr.range = range;
    Udyr.cc = cc;
    Udyr.mobility = mobility;
    Udyr.damage = damage;
    Udyr.heal = heal;
    Udyr.shield = shield;
    
    i++;
    Champs.push_back(Udyr);
    
	//Urgot
    Champion Urgod;
    Urgod.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Urgod.tank = tank;
    Urgod.range = range;
    Urgod.cc = cc;
    Urgod.mobility = mobility;
    Urgod.damage = damage;
    Urgod.heal = heal;
    Urgod.shield = shield;
    
    i++;
    Champs.push_back(Urgod);
    
	//Varus
    Champion Varus;
    Varus.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Varus.tank = tank;
    Varus.range = range;
    Varus.cc = cc;
    Varus.mobility = mobility;
    Varus.damage = damage;
    Varus.heal = heal;
    Varus.shield = shield;
    
    i++;
    Champs.push_back(Varus);
    
	//Vayne
    Champion Vayne;
    Vayne.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Vayne.tank = tank;
    Vayne.range = range;
    Vayne.cc = cc;
    Vayne.mobility = mobility;
    Vayne.damage = damage;
    Vayne.heal = heal;
    Vayne.shield = shield;
    
    i++;
    Champs.push_back(Vayne);
    
	//Veigar
    Champion Veigar;
    Veigar.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Veigar.tank = tank;
    Veigar.range = range;
    Veigar.cc = cc;
    Veigar.mobility = mobility;
    Veigar.damage = damage;
    Veigar.heal = heal;
    Veigar.shield = shield;
    
    i++;
    Champs.push_back(Veigar);
    
	//Vel'Koz
    Champion Vel;
    Vel.name = Champions[i];
getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Vel.tank = tank;
    Vel.range = range;
    Vel.cc = cc;
    Vel.mobility = mobility;
    Vel.damage = damage;
    Vel.heal = heal;
    Vel.shield = shield;
    
    i++;
    Champs.push_back(Vel);
    
	//Vi
    Champion Vi;
    Vi.name = Champions[i];
	getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Vi.tank = tank;
    Vi.range = range;
    Vi.cc = cc;
    Vi.mobility = mobility;
    Vi.damage = damage;
    Vi.heal = heal;
    Vi.shield = shield;
    
    i++;
    Champs.push_back(Vi);
    
	//Viktor
    Champion Viktor;
    Viktor.name = Champions[i];
	getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Viktor.tank = tank;
    Viktor.range = range;
    Viktor.cc = cc;
    Viktor.mobility = mobility;
    Viktor.damage = damage;
    Viktor.heal = heal;
    Viktor.shield = shield;
    
    i++;
    Champs.push_back(Viktor);
    
	//Vladimir
    Champion Vlad;
    Vlad.name = Champions[i];
	getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Vlad.tank = tank;
    Vlad.range = range;
    Vlad.cc = cc;
    Vlad.mobility = mobility;
    Vlad.damage = damage;
    Vlad.heal = heal;
    Vlad.shield = shield;
    
    i++;
    Champs.push_back(Vlad);
    
	//Volibear
    Champion Voli;
    Voli.name = Champions[i];
	getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Voli.tank = tank;
    Voli.range = range;
    Voli.cc = cc;
    Voli.mobility = mobility;
    Voli.damage = damage;
    Voli.heal = heal;
    Voli.shield = shield;
    
    i++;
    Champs.push_back(Voli);
    
	//Warwick
    Champion WW;
    WW.name = Champions[i];
	getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    WW.tank = tank;
    WW.range = range;
    WW.cc = cc;
    WW.mobility = mobility;
    WW.damage = damage;
    WW.heal = heal;
    WW.shield = shield;
    
    i++;
    Champs.push_back(WW);
    
	//Wukong
    Champion Wu;
    Wu.name = Champions[i];
	getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Wu.tank = tank;
    Wu.range = range;
    Wu.cc = cc;
    Wu.mobility = mobility;
    Wu.damage = damage;
    Wu.heal = heal;
    Wu.shield = shield;
    
    i++;
    Champs.push_back(Wu);
    
	//Xayah
    Champion Xayah;
    Xayah.name = Champions[i];
	getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Xayah.tank = tank;
    Xayah.range = range;
    Xayah.cc = cc;
    Xayah.mobility = mobility;
    Xayah.damage = damage;
    Xayah.heal = heal;
    Xayah.shield = shield;
    
    i++;
    Champs.push_back(Xayah);
    
	//Xerath
    Champion Xerath;
    Xerath.name = Champions[i];
	getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Xerath.tank = tank;
    Xerath.range = range;
    Xerath.cc = cc;
    Xerath.mobility = mobility;
    Xerath.damage = damage;
    Xerath.heal = heal;
    Xerath.shield = shield;
    
    i++;
    Champs.push_back(Xerath);
    
	//Xin Zhao
    Champion Xin;
    Xin.name = Champions[i];
	getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Xin.tank = tank;
    Xin.range = range;
    Xin.cc = cc;
    Xin.mobility = mobility;
    Xin.damage = damage;
    Xin.heal = heal;
    Xin.shield = shield;
    
    i++;
    Champs.push_back(Xin);
    
	//Yasuo
    Champion Yasuo;
    Yasuo.name = Champions[i];
	getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Yasuo.tank = tank;
    Yasuo.range = range;
    Yasuo.cc = cc;
    Yasuo.mobility = mobility;
    Yasuo.damage = damage;
    Yasuo.heal = heal;
    Yasuo.shield = shield;
    
    i++;
    Champs.push_back(Yasuo);
    
	//Yorick
    Champion Yorick;
    Yorick.name = Champions[i];
	getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Yorick.tank = tank;
    Yorick.range = range;
    Yorick.cc = cc;
    Yorick.mobility = mobility;
    Yorick.damage = damage;
    Yorick.heal = heal;
    Yorick.shield = shield;
    
    i++;
    Champs.push_back(Yorick);
    
	//Zac
    Champion Zac;
    Zac.name = Champions[i];
	getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Zac.tank = tank;
    Zac.range = range;
    Zac.cc = cc;
    Zac.mobility = mobility;
    Zac.damage = damage;
    Zac.heal = heal;
    Zac.shield = shield;
    
    i++;
    Champs.push_back(Zac);
    
	//Zed
    Champion Zed;
    Zed.name = Champions[i];
	getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Zed.tank = tank;
    Zed.range = range;
    Zed.cc = cc;
    Zed.mobility = mobility;
    Zed.damage = damage;
    Zed.heal = heal;
    Zed.shield = shield;
    
    i++;
    Champs.push_back(Zed);
    
	//Ziggs
    Champion Ziggs;
    Ziggs.name = Champions[i];
	getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Ziggs.tank = tank;
    Ziggs.range = range;
    Ziggs.cc = cc;
    Ziggs.mobility = mobility;
    Ziggs.damage = damage;
    Ziggs.heal = heal;
    Ziggs.shield = shield;
    
    i++;
    Champs.push_back(Ziggs);
    
	//Zilean
    Champion Zilean;
    Zilean.name = Champions[i];
	getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Zilean.tank = tank;
    Zilean.range = range;
    Zilean.cc = cc;
    Zilean.mobility = mobility;
    Zilean.damage = damage;
    Zilean.heal = heal;
    Zilean.shield = shield;
    
    i++;
    Champs.push_back(Zilean);
	
	//Zyra
    Champion Zyra;
    Zyra.name = Champions[i];
	getline(myfile, line);
    x = 0;
    while(!isdigit(line[x]))
    {x++;}
    tank = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    range = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    cc = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    mobility = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    damage = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    heal = line[x];
x++;
    while(!isdigit(line[x]))
    {x++;}
    shield = line[x];
x++;
    Zyra.tank = tank;
    Zyra.range = range;
    Zyra.cc = cc;
    Zyra.mobility = mobility;
    Zyra.damage = damage;
    Zyra.heal = heal;
    Zyra.shield = shield;
    
    Champs.push_back(Zyra);
    
    cout << Champs[136].name << endl;

	myfile.close();

};

void selectTank(){};
void selectADC(){};
void selectMid(){};
void selectJG(){};
void selectSupp(){};

void existingPlayers;

vector<Player> Players; //In reality this is a small scope program. Vector should suffice. If used for large scale, change to a hashmap

Player newPlayer(string name)
{
	Player *temp = new Player; //New temp Player
	string champ;
	temp->name = name; //Add the player's name
	cout << "Please enter each champion in " << name << "'s pool, press enter between each. When you're done, type in quit" << endl;
	while(champ != "quit")
	{
		cin >> champ;
		if(find(Champions.begin(), Champions.end(), champ) != Champions.end())
			temp->pool.push_back(champ);
		else if(champ == "quit")
		    continue;
		else
		{
			cout << "Please enter a valid Champion/correct the incorrect name" << endl;
			cout << "You entered: " << champ << endl;
		}
	}; //Quit phrase is quit
	
	return *temp;

};


void store()
{
	string name;
	cout << "Please enter the Player's name" << endl;
	cin.ignore();
	getline(cin, name);
	Players.push_back(newPlayer(name));
	cout << "Player has been stored successfully" << endl;
};

void display()
{
    string name;
	cout << "Please enter the Player's name" << endl;
	cin.ignore();
	getline(cin, name);
    bool found = false;
    for (unsigned int i = 0; i < Players.size(); i++)
	{
	    if(Players[i].name == name)
	        found = true;
	}
	   
	if(found)
	{
	        cout << "The player " << name << " has been found" << endl;
	        cout << "Their champion pool is " ;
	}
	else
	    cout << "I'm sorry but the player " << name << " was not found in your database. Please check your spelling for errors." << endl;
};

void teamBuilder()
{
	cout << "Based on your champion pools, this is the best general composition for your selection: " << endl;
};

int main()
{
	initializeChamps();
	//Menu
	//M-1: Store Player Data
	//M-2: Find Player Data
	//M-3: Create Team
		//Load Players into their roles
		//Determine what kind of comp; Tanky, CC-Heavy, Balanced, Protect the ADC
		//Select Champions depending on certain value requirements for the team
	//M-4: Quit
	
    	int input;
	do
	{
		cout << "What would you like to do?" << endl;
		cout << "To store player data and champion pools, enter 1" << endl;
		cout << "To display player data and champion pools, enter 2" << endl;
		cout << "To create a team, enter 3" << endl;
		cout << "To exit, enter 4" << endl;
		
		cin >> input;
		
		if (input == 1)
			store();
		if (input == 2)
			display();
		if (input == 3)
		    teamBuilder();
		

	} while (input != 4);


	cout << "Goodbye" << endl;



    return 0;
}



